# Tasks

- [x] Update the `rust-workspace` spec delta to reflect the actual crate directories.
- [x] Run `ito validate 015-03_update-rust-workspace-specs --strict`.
- [x] After approval, archive the change to apply the spec update.
