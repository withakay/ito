# CLI

## Purpose
Command-line interface for Ito (`ito-cli` crate). Argument parsing, command dispatch, output formatting, bridge types, and user-facing CLI behaviour.

## Scope
- Argument parsing, command dispatch, output formatting, bridge types, and CLI behaviour.

## Changes
- [ ] 016-10_cli-type-safe-args
