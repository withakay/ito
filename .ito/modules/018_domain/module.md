# Domain

## Purpose
Domain models and repositories for Ito (`ito-domain` crate). Pure domain types, value objects, entities, and repository traits with no framework dependencies.

## Scope
- -

## Changes
