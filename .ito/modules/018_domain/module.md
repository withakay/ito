# Domain

## Purpose
Domain models and repositories for Ito (`ito-domain` crate). Pure domain types, value objects, entities, and repository traits with no framework dependencies.

## Scope
- Pure domain types, value objects, and entities
- Repository traits and port definitions
- Domain validation and business rules
- No framework or infrastructure dependencies

## Changes
