# Web

## Purpose
Web server and UI for Ito (`ito-web` crate). Local documentation browser, artifact viewer, and API endpoints.

## Scope
- Local web server for documentation browsing
- Artifact viewer and API endpoints
- UI rendering and static asset serving

## Changes
