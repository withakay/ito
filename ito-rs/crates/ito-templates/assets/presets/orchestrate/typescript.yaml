name: typescript

gate_order:
  - apply-complete
  - format
  - lint
  - tests
  - style
  - code-review
  - security-review

gates:
  apply-complete: {}
  format:
    tool: "npm run format"
  lint:
    tool: "npm run lint"
  tests:
    tool: "npm test"
  style: {}
  code-review: {}
  security-review: {}

recommended_skills: []

agent_roles:
  apply-worker: "typescript-engineer"
  review-worker: "typescript-code-reviewer"
  security-worker: ""
